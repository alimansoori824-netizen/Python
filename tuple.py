# tuple is
# collection of element
# enclosed with ()
# comma (,) seperated elements
# ordered collection
# indexing support
# slicing supported
# duplcate allowed
# immutable in nature

# only methods which doesnt change the structure are applicable in tuple
# index()
# count()

a=eval(input("enter tuple"))
print(a)
print(a.index(3))
print(a.count(5))#counts how many times a value is occuring

# __ means magic methods or dinder method